diff --git a/node_modules/react-native-css/dist/commonjs/babel/react-native-web.js b/node_modules/react-native-css/dist/commonjs/babel/react-native-web.js
index 9414040..3670dd3 100644
--- a/node_modules/react-native-css/dist/commonjs/babel/react-native-web.js
+++ b/node_modules/react-native-css/dist/commonjs/babel/react-native-web.js
@@ -10,6 +10,13 @@ var _path = require("path");
 var _allowedModules = require("./allowedModules.js");
 function parseReactNativeWebSource(source, filename) {
   if (source.startsWith(".")) {
+    // Patch: do not rewrite react-native-web's own internal component imports.
+    // Wrapping them pulls react-native-css component wrappers (which require
+    // "react-native") back into react-native-web's own initialization, creating
+    // a circular import that crashes the web runtime (e.g. FlatList).
+    if (typeof filename === "string" && filename.includes("react-native-web")) {
+      return;
+    }
     source = (0, _path.resolve)(filename, source);
     const internalPath = source.split("react-native-web/dist")[1];
     if (!internalPath) {
diff --git a/node_modules/react-native-css/dist/module/babel/react-native-web.js b/node_modules/react-native-css/dist/module/babel/react-native-web.js
index ced94aa..8ba586a 100644
--- a/node_modules/react-native-css/dist/module/babel/react-native-web.js
+++ b/node_modules/react-native-css/dist/module/babel/react-native-web.js
@@ -4,6 +4,13 @@ import { resolve } from "path";
 import { allowedModules } from "./allowedModules.js";
 function parseReactNativeWebSource(source, filename) {
   if (source.startsWith(".")) {
+    // Patch: do not rewrite react-native-web's own internal component imports.
+    // Wrapping them pulls react-native-css component wrappers (which require
+    // "react-native") back into react-native-web's own initialization, creating
+    // a circular import that crashes the web runtime (e.g. FlatList).
+    if (typeof filename === "string" && filename.includes("react-native-web")) {
+      return;
+    }
     source = resolve(filename, source);
     const internalPath = source.split("react-native-web/dist")[1];
     if (!internalPath) {
