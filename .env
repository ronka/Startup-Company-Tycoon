# PostHog analytics. These are the built-in defaults (also hardcoded in
# src/analytics/config.ts) — override here to point a build at another project.
# The project API key is a PUBLIC client key (write-only), safe to commit.
EXPO_PUBLIC_POSTHOG_API_KEY=phc_rcTNz7PrzGa8fupe5DFvHGWeMmadRjHt93WWuM2SFxgp
EXPO_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# RevenueCat (IAP week packs, Task 3/5). iOS public SDK key for the
# "Startup Tycoon iOS" app (project projb69142f2) — a PUBLIC client key,
# safe to commit. Never put the RC *secret* key here. The RC app is still
# named after the old app name; renaming it there is cosmetic and the
# product IDs (com.ronkaa.startuptycoon.*) stay as they are.
EXPO_PUBLIC_REVENUECAT_IOS_API_KEY=appl_aADTuKHSudKtuKXFkoYoROErLhg
